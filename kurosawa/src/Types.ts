interface PageLinkType {
  title: string;
  url: string;
  vidSrc: string;
}
interface VideoType {
  decadeTitle: string;
  id: string;
  src: string;
}
interface IndexActiveType {
  index: number;
  isActive: boolean;
}
